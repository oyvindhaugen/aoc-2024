{
  "devDependencies": {
    "@types/dotenv": "^6.1.1",
    "@types/node": "^22.8.2",
    "gts": "^6.0.2",
    "typescript": "^5.6.3"
  },
  "scripts": {
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm.cmd run compile",
    "pretest": "npm.cmd run compile",
    "posttest": "npm.cmd run lint"
  },
  "type": "module",
  "dependencies": {
    "dotenv": "^16.4.5",
    "fs": "^0.0.1-security"
  }
}
